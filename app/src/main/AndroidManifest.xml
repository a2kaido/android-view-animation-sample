<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.a2kaido.viewanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ViewAnimationActivity"
            android:label="@string/title_activity_view_animation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.activity.ObjectAnimatorActivity" />
        <activity android:name=".ui.activity.AnimatorSetActivity" />
        <activity android:name=".ui.activity.ValueAnimatorActivity" />
        <activity android:name=".ui.activity.SpringAnimationActivity"></activity>
    </application>

</manifest>